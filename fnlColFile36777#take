 tmpCol and append on fnlCol with newlines as tabs
 tmpCol and append on fnlCol with newlines as tabs
 tmpCol and append on fnlCol with newlines as tabs
 tmpCol and append on fnlCol with newlines as tabs
